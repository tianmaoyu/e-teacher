# 2026-09-14 · SpeakMate（e-teacher）项目日志

## 项目定位
AI 英语口语教练 SaaS。Android 原生 App + 自建计费网关，语音层 OpenAI GPT-Live-1（wss://api.openai.com/v1/live/sessions，$0.05/分钟）。App 不持有任何 Key，只认自签发的兑换码 token。

## 已完成
- `DESIGN.md` / `docs/PROTOCOL.md` / `docs/DEPLOY.md`：架构、事件协议、上线手册
- `server/`：Node.js 计费网关（唯一依赖 ws）。token 鉴权、按秒扣费（余额尽自动断线）、
  OpenAI WebSocket 透明代理 + session.start 强制覆写、ledger 流水、兑换码、管理 API。
  冒烟测试 40 项断言全过（`server/test/smoke.mjs`）
- `android/`：Kotlin + Compose M3。包名 com.tianmaoyu.speakmate。
  AudioRecord 24kHz PCM16 采集 + AudioTrack 流播（AEC）、OkHttp WebSocket、
  双语字幕、激活/通话/设置三屏、前台服务
- 本地 `assembleDebug` 构建成功，APK 13MB
- 已推送 https://github.com/tianmaoyu/e-teacher （SSH 通道，main 分支）

## 关键环境事实（重要，复用价值高）
- 本地工具链：JDK21 `/Users/eric/.toolchains/jdk-21`、Gradle 9.7.1 `/Users/eric/.toolchains/gradle-9.7.1`、
  Android SDK `/Users/eric/.android-sdk`（platforms 36/37、build-tools 36.1.0/37.0.0）
- 构建命令：`cd android && JAVA_HOME=... ANDROID_HOME=... /Users/eric/.toolchains/gradle-9.7.1/bin/gradle assembleDebug`
- **AGP 9 坑（2026 生态已全量迁移）**：
  - AGP 9.0+ 内置 Kotlin，**必须移除 `org.jetbrains.kotlin.android` 插件**，否则报
    "Cannot add extension with name 'kotlin'"
  - AGP 9.4.0 内置 KGP 2.2.10；用新 Kotlin 需在顶层 build.gradle.kts 加
    `buildscript { dependencies { classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:X.Y.Z") } }`
  - 最低 Gradle 9.1 / JDK 17；`isMinifyEnabled` 等 legacy DSL 仍可用
  - Compose BOM 2026.09 / core-ktx 1.19 / okhttp 5.5 要求 compileSdk 37
  - `android.nonTransitiveRClass` 开关已移除（默认即非传递）
- 推送凭证：GitHub MCP 集成只读（建仓/推送均 403）；钥匙串里的 PAT 已失效；
  **SSH key `~/.ssh/id_rsa`（口令用户掌握）可用**：起独立 agent
  `ssh-agent -a /tmp/speakmate-ssh.sock` + expect 输口令 ssh-add，然后
  `SSH_AUTH_SOCK=/tmp/speakmate-ssh.sock git push git@github.com:...`
- 本机出口 IP 对 api.github.com 匿名限流频繁耗尽，用
  `https://github.com/<owner>/<repo>/actions/workflows/<f>.yml/badge.svg` 查 CI 状态

## 待办
- Android CI（.github/workflows/android.yml）失败中，正在排查：本地已过 debug，
  疑点在 assembleRelease（R8）或 JDK 17 差异。server CI 已通过
- CI 通过后打 tag v1.0.0 发 Release（workflow 已配好 tag 触发 + APK 产物）
- 用户侧后续：部署 server 到 VPS、生成兑换码、配置 App 默认网关地址（构建参数
  `-Pspeakmate.gateway=https://...`）
